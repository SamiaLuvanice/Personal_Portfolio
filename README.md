# 🚀 Personal Portfolio

Portfólio pessoal moderno desenvolvido com React, TypeScript e Tailwind CSS, com animações suaves e design responsivo.

## ✨ Stack de Tecnologia

- **React 19** - Biblioteca para interfaces de usuário
- **TypeScript** - Superset tipado do JavaScript
- **Vite** - Build tool e dev server ultra-rápido
- **Tailwind CSS v4** - Framework CSS utility-first
- **Framer Motion** - Biblioteca de animações para React
- **Lucide React** - Ícones SVG limpos e consistentes
- **React Router DOM** - Roteamento para SPAs

## 🎨 Design System

### Fontes
- **Sora** - Fonte principal (sans-serif)
- **Space Grotesk** - Fonte de display (títulos)

### Tokens de Cores (HSL)
- `--background` - Cor de fundo principal
- `--foreground` - Cor de texto principal
- `--primary` - Cor primária (laranja vibrante)
- `--primary-glow` - Variação com brilho
- `--accent` - Cor de destaque (magenta)
- `--tertiary` - Cor terciária (ciano)
- `--muted` - Cores neutras
- Suporte completo para dark mode

### Recursos de Design
- Sistema de cores HSL configurável
- Gradientes personalizados
- Efeitos glassmorphism
- Animações customizadas (float, fade, slide, scale)
- Sombras suaves e glow effects
- Transições smooth e bounce

## 🚀 Como Executar

### Instalação
```bash
npm install
```

### Desenvolvimento
```bash
npm run dev
```

### Build de Produção
```bash
npm run build
```

### Preview da Build
```bash
npm run preview
```

### Lint
```bash
npm run lint
```

## 📁 Estrutura do Projeto

```
src/
├── assets/          # Imagens, ícones e arquivos estáticos
├── pages/           # Páginas da aplicação
│   ├── Index.tsx    # Página principal
│   └── NotFound.tsx # Página 404
├── App.tsx          # Componente raiz
├── main.tsx         # Entry point
└── index.css        # Estilos globais e tokens
```

## ⚙️ Configuração

O projeto está configurado com:
- ESLint para análise estática de código
- TypeScript com configurações strict
- Tailwind CSS v4 com plugin Vite
- Suporte a path aliases via @types/node

## 📝 Status do Setup

✅ Vite + React + TypeScript  
✅ Tailwind CSS v4  
✅ Fontes configuradas (Sora + Space Grotesk)  
✅ Tokens de cores HSL definidos  
✅ Dependências instaladas (framer-motion, lucide-react, react-router-dom)  
✅ Sistema de design base configurado
